const AdminLoginBtn = () => {
  return (
    <button
      onClick={() => (window.location.href = "http://localhost:3000/chip_time")}
    >
      Admin Login
    </button>
  );
};

export default AdminLoginBtn;
